{
  "name": "daigo-backend",
  "version": "1.0.0",
  "description": "代採購平台的後端服務",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "npm run prisma:migrate && node server.js",
    "dev": "nodemon server.js",
    "prisma:dev": "prisma migrate dev",
    "prisma:migrate": "prisma migrate deploy",
    "prisma:generate": "prisma generate",
    "prisma:seed": "node prisma/seed.js",
    "db:create-admin": "echo \"'db:create-admin' 已被 'prisma:seed' 取代。請在 .env 中設定 ADMIN_USERNAME 和 ADMIN_PASSWORD 並執行 npm run prisma:seed\""
  },
  "dependencies": {
    "@prisma/client": "^5.15.0",
    "@sendgrid/mail": "^8.0.0",
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "joi": "^18.0.1",
    "jsonwebtoken": "^9.0.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.1",
    "prisma": "^5.15.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
